apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    azure.workload.identity/client-id: ${platformExternalSecretsClientId}
    azure.workload.identity/tenant-id: ${platformTenantId}
  name: ${platformExternalSecretsServiceAccountName}
  namespace: ${platformNamespace} 